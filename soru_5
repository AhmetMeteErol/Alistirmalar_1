# This programme returns palindromic numbers 
# with four digits like 1221, 3223, 4114

palindromics = []           # We identify an empty list

for n in range(1000,10000):
    if (str(n)[0:3]) == (str(n)[-1:-4:-1]):
        palindromics.append(n)
print(palindromics)
print(f"We have {len(palindromics)} palindromic numbers with 4 digits. ")
